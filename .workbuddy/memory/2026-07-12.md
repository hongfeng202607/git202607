# 2026-07-12 工作记录

## 初始化 git 仓库并推送到 GitHub
- 目标文件夹：`D:\git202607`（原本为空）
- 操作：
  - `git init -b main`，配置全局 user.name=`hongfeng202607`、user.email=`hongfeng.ji@revvity.com`
  - 空仓库补 `README.md` 作为首个提交（commit `9a8a4af`）
  - 添加远程 `origin` → `https://github.com/hongfeng202607/git202607.git`
  - 用 classic PAT（用户第二次提供的 `ghp_...`）成功 `git push -u origin main`
- 踩坑：用户第一次给的是 fine-grained token（`github_pat_`），推送 403 denied；改为 classic token（repo 权限）后成功。
- 安全：推送后已把 token 从 `remote.origin.url` 清除，并告知用户及时 revoke token。
- 已将该流程沉淀为技能 `github-push-token`（用户级 skills）。

## 二次推送：工作汇报系统源码
- 用户往 `D:\git202607` 放入整个「工作汇报系统」（慧报空间）文件夹后要求再次推送。
- 踩坑：`git add -A` 默认吞掉了 node_modules（数千文件）、`jwt-secret.txt`（密钥！）、`backend/backup/*.sql`（数据库备份）。幸好未 commit/push，密钥未泄露。
- 处理：`git reset` 取消暂存 → 新建 `.gitignore`（忽略 node_modules/、jwt-secret.txt、*.env、backend/backup/、*.sql（保留 database/init.sql）、*.bak*） → 重新 add，文件数从数千降到 78。
- 提交 `cdcbd66`，用同一 classic token 成功 push，事后清除远程 token。
- 教训：给含 Node 项目的文件夹初始化 git 时，必须先建 .gitignore 再 add，重点排除 node_modules 和密钥文件。已同步更新技能 github-push-token。
