# 2026-05-22 工作日志

## 全面代码审查
对智行知识库 V07.5.12 进行了全面的代码审查（后端 Python + 前端 HTML/JS + 工具脚本）。

### 发现的严重问题
1. **恢复备份.py 配置错误**：`DB_CONFIG` 中 user 写成了 `'sa'` 而非 `'postgres'`，导致恢复失败
2. **SQL注入风险**：`auth.py` 第166行使用 f-string 拼接用户名到 SQL WHERE 子句
3. **密码硬编码**：PostgreSQL 密码 `123456` 明文硬编码在多个文件中
4. **N+1查询**：搜索结果循环中逐条查询附件状态
5. **前端 HTML 过大**：index.html 189KB 内嵌所有 CSS+JS

### 主要建议
- 拆分 index.html 为 CSS/JS/HTML
- 创建 requirements.txt
- 数据库密码迁移到环境变量
- 统一数据库连接模式
- 添加关键词搜索长度限制

审查报告已写入：`.workbuddy/overview.md`

## 修复完成（2026-05-22 18:09）
按红枫要求修复了以下3个问题：
1. ✅ **恢复备份.py**：`user` 从 `'sa'` → `'postgres'`
2. ✅ **auth.py SQL注入**：f-string 拼接改为参数化查询（`%s` + params 列表）
3. ✅ **创建 requirements.txt**：flask/psycopg2-binary/pandas/openpyxl/requests

## 批量修复高性价比项（2026-05-22 18:13）
按红枫要求修复了高性价比的4项：
4. ✅ **编辑记录添加存在性检查**：`knowledge.py` edit() 增加 SELECT 校验，不存在时返回错误
5. ✅ **关键词长度限制**：`tokenize_keywords()` 统一限100字符，防恶意超长查询
6. ✅ **N+1 查询修复**：搜索结果附件状态改为一次 IN 查询，两处都改了
7. ✅ **抽取公共分词函数**：`db.py` 新增 `tokenize_keywords()` + `build_like_conditions()`，`knowledge.py` 和 `ai.py` 调用同一函数

## 中等回报优化（2026-05-22 18:17）
按红枫要求修复了3项中等回报优化：
8. ✅ **数据库密码环境变量化**：`db.py` / `恢复备份.py` / `手动备份.py` 的 DB_CONFIG 密码从 `os.environ.get('KB_DB_PASSWORD', '123456')` 读取，支持 KB_DB_HOST / KB_DB_PORT / KB_DB_USER / KB_DB_PASSWORD / KB_DB_NAME 全套环境变量
9. ✅ **统一数据库连接风格**：`login_required` 装饰器修复连接泄露（早期 return 未关连接）；`auth.py` 中 `_has_permission` / `get_user_permissions` / `login` / `logout` / `reset_pwd` / `user_list` 改用 `with get_db_connection() as conn`；`role.py` 中 `get_permissions` / `get_roles` 同样转换
10. ✅ **拆分 index.html**：189KB → 34KB（HTML骨架）+ 40KB（static/style.css）+ 112KB（static/app.js），静态文件通过 app.py 新增 `/static/` 路由提供

## 代码异味优化（2026-05-22 18:22）
按红枫要求修复了3项代码异味：
11. ✅ **SMELL-2 启动连接复用**：`init_rbac()` / `init_admin()` / `init_database_data()` / `auto_import_excel()` 统一加可选 `conn` 参数，`app.py` 启动时一个连接搞定全部初始化（原来建5个连接）
12. ✅ **SMELL-4 op_log优雅化**：抽离 `_get_current_username()` 辅助函数，替代原来 try-except RuntimeError 的粗糙写法
13. ✅ **SMELL-7 版本号统一**：`launcher.py` / `index.html` / `新建 文本文档.py` / `数据库工具_GUI.py` / `数据库说明.txt` / `项目目录说明.txt` 全部统一为 V07.5.12

## 前端 UI/UX 改进（2026-05-22 18:27）
按红枫要求优化了5项 UI/UX：
14. ✅ **UX-1 统一权限控制**：编辑/删除/恢复/永久删除按钮改为条件渲染（无权限直接不生成HTML），移除 perm-disabled/role-user/role-editor 等 CSS 旧方案
15. ✅ **UX-2 搜索防抖+加载骨架屏**：输入框 800ms 防抖自动搜索，搜索结果显示 shimmer 骨架屏动画
16. ✅ **UX-3 表单校验提示**：标题/核心内容输入框下方增加行内红色提示文字（questionHint / solutionHint），保存时自动校验并聚焦
17. ✅ **UX-4 移动端适配**：AI 智能助手面板在 <992px 时改为 100vw，max-width:380px
18. ✅ **UX-5 上传取消**：XHR 上传增加取消按钮（调用 xhr.abort()），进度条旁显示"取消"链接

## 附件完整性保护（2026-05-22 18:35）
19. ✅ **未保存附件离开提醒**：`beforeunload` 监听 newUploadedFiles 长度，有未保存附件时弹窗确认
20. ✅ **无主附件自动清理**：`cleanup_orphan_uploads()` 扫描 uploads/ 中超过24小时、未在 attachments 表注册的文件并删除，服务启动时自动执行

## 21:04 全项目清理"默认分类"残留
- `V07.5.11操作说明书.html` 中2处"默认分类"引用已清除
- `数据库工具_GUI.py:188` 兜底 `"默认分类"` → `"其他"`
- 全项目零"默认分类"残留

## 21:06 文档版本号统一
- `V07.5.11具体方案.html` / `操作说明书.html` / `版本说明.html` 三份文档共44处内容更新 + 文件名重命名，全部改为 V07.5.12

## 21:21 备份/恢复整合到 tools.html
- 新增 3 个 API：`/api/backup/manual` / `/api/backup/list` / `/api/backup/restore`
- `tools.html` 界面增加手动备份按钮、备份文件列表（含大小/时间）、恢复按钮（双重确认）
- 自动检测登录后刷新备份列表
- 已清理 `fix_roles.py`（一次性脚本）、`新建 文本文档.py`（launcher 重复副本）
- `项目目录说明.txt` 同步更新

## 21:39 tools.html 增加建表 + 备份列表折叠
- 新增 API `POST /api/backup/init_tables`：空库初始化表结构 + RBAC + 管理员（误删库后重建用）
- tools.html 新增「初始化数据库表结构」按钮，带二次确认
- 备份列表默认折叠，显示最近3条，超3条显示"展开全部"按钮

## 22:00 全面二次审查（3 Agent 并行）
- 后端、前端、工具脚本三路并行审查，共发现 **30+ 项问题**
- 🔴 紧急 5 项：XSS (origin_name)、Token localStorage 明文、密码 Base64、PostgreSQL 路径硬编码、shell=True 注入风险
- 🟠 高优 6 项：auth.py 10处连接未统一、数据权限无缓存、AI Key 明文存储、缺CSRF、saveEdit无校验
- 🟡 中优 6 项：权限缓存无刷新、搜索loading缺失、13个全局变量、分类弹窗重建DOM、恢复备份无进度通知
- 🟢 低优 13 项：CSS transition:all滥用、素材未压缩、依赖不完整等
- 完整报告：`.workbuddy/overview.md`

## 已知修复状态（前次审查已修复项确认未退化）
- SQL注入 ✅ / 密码环境变量化 ✅ / N+1查询 ✅ / 公共分词 ✅ / 连接风格（部分）✅

## 22:00 紧急漏洞修复（5项）
红枫确认开始修复后，完成以下5项：
1. ✅ **S1 XSS漏洞**：`static/app.js:1612` `d.origin_name` 未转义 → 加 `escapeHtml()`
2. ✅ **S4 PostgreSQL路径硬编码**：`app.py` + `手动备份.py` + `恢复备份.py` 共6处 `F:\PostgreSQL\bin\` → 改用 `os.environ.get('KB_PG_BIN', ...)` 模块级变量
3. ✅ **S5 shell=True注入风险**：`app.py:187` 字符串拼接传参 → 改为列表传参 `subprocess.run([exec, arg1, ...])`，2个脚本同步修复
4. ✅ **P1 auth.py连接统一**：`user_add/user_reset/user_status/user_delete/user_role` 共5个函数、10处手动 `conn.close()` 全部改为 `with get_db_connection() as conn`
5. ✅ **2个备用脚本同步**：`手动备份.py`、`恢复备份.py` 同步修复路径硬编码 + shell=True
