# 2026-05-28 工作日志

## Linux 部署适配
- 创建 `start.sh` — Linux 启动脚本，自动检测 Node.js、安装依赖、构建前端、生成 JWT 密钥
- 创建 `backend/ecosystem.config.js` — PM2 生产环境配置
- 修改 `backend/backup.js` — 自动适配 Linux/Windows 路径（which pg_dump 自动查找）
- 生成两份部署手册 HTML
- `慧报空间-Linux部署手册.html` — 通用 Linux 部署（Ubuntu + CentOS 双版本）
- `慧报空间-Linux部署手册-拷贝版.html` — 针对"整个项目从 Windows 拷贝到 Linux"场景的精简版

## 部署结论
项目代码完全跨平台，只需删掉 Windows 的 node_modules 在 Linux 上重新 npm install 即可。数据库密码通过环境变量 `DB_PASSWORD` 传入，无需改代码。

## 系统管理页面（完整新增）
- 数据库：`sys_user` 加 `token_version`（清退）+ `last_active_time`（在线追踪），新建 `system_log` 表
- 后端 `middleware/auth.js`：JWT 校验 token_version + 异步更新 last_active_time
- 后端 `routes/auth.js`：登录 JWT 加 token_version，记录登录日志
- 后端 `routes/system.js`：7 个接口（在线用户/清退/备份/恢复/日志列表/AI统计）
- 后端 `routes/record.js`：提交/撤回记录记日志
- 后端 `routes/report.js`：AI 生成记日志
- 后端 `routes/audit.js`：通过/退回记日志
- 前端 `admin/System.vue`：三 Tab（在线用户、数据备份、系统日志）
- 路由 `router/index.js` + 菜单 `Layout.vue` 添加"系统管理"入口

## AI 配置脱敏
- 后端 GET /api/ai-config 返回 api_key 前4+****+后4
- 后端 PUT /api/ai-config 检测 **** 时跳过更新
- 后端 `/ai-proxy/test` 改为从数据库读取 api_key（不依赖前端传参）
- 前端 AiConfig.vue：去除小眼睛，改为只读显示脱敏值 + 修改按钮弹出对话框

## 文档批量更新
- 更新了 9 个文档文件：操作手册.html、项目文件说明.txt、数据初始化指南.md、数据库字典、index.html、Linux部署手册、Windows手册（归档标记）
- 跳过：工作汇报系统方案.docx、常用提示词.txt
