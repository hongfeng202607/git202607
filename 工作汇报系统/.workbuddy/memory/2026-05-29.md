# 2026-05-29 工作日志

## 系统管理模块新增
- 新增菜单项 + 页面 `admin/System.vue`（在线用户、数据备份/删除/恢复、系统日志、日志清理）
- 数据库：`sys_user` 加 `token_version`（清退）、`last_active_time`（在线追踪）
- 新建 `system_log` 表，记录关键操作日志
- 后端 `middleware/auth.js`：JWT 校验 token_version + 异步更新 last_active_time
- 后端 `routes/system.js`：7 个接口（在线用户/清退/备份/恢复/日志列表/AI统计/日志清理/删除备份）
- 自动日志清理：每天凌晨 3 点删除 30 天前日志
- 备份文件 gzip 压缩，备份路径修复为 backend/backup/

## Markdown 渲染
- 安装 `marked` 包，Reports.vue + Audit.vue 所有展示区改为 v-html 渲染
- AI 提示词追加 Markdown 格式要求 + 禁止外层标题
- CSS 样式：h2/h3/h4 字号+加粗调整

## 审批穿透修复
- `audit.js` GET /pending：穿透查询增加 `cr.receiver_id = ANY(树)` 条件，防止显示提交给树外接收人的汇报
- 修复前：侯轶楠可看到赵俊（赵俊→王南→侯轶楠穿透），但赵俊的汇报实际提交给杨唯书（不在树中）

## 通知功能
- Dashboard 通知改为仅显示未读（unread_only=1），已读自动隐藏
- 未读蓝底高亮 + 已读灰色淡化 CSS

## Reports.vue 重建
- 之前 Write 覆盖导致丢失功能，已逐项恢复：
  - 工作记录展开/折叠
  - 加载按钮（按周期范围调用 /reports/work-records）
  - 包含下属汇报复选框
  - 原生 checkbox 替代 el-checkbox-group（解决截断问题）
  - box-sizing: border-box + overflow-x: hidden（解决文本溢出）
  - 状态筛选开关（全部/草稿/进行中/已通过）
  - API 路径修正：/reports/create → /reports，/reports/ai-generate 路由加回
  - save_as_draft 参数修正

## Linux 部署（正式版）
- RHEL 10 / CentOS Stream 10
- PostgreSQL 16（dnf install）
- Node.js 22（NodeSource）
- systemd 服务 huibao，端口 8901，开机自启 + 崩溃自恢复
- 防火墙开放 8901/tcp
- 外部访问：http://10.61.190.27:8901

## 文档更新
- 操作手册.html：新增系统管理章节
- 数据库字典.html：新增 system_log 表 + sys_user 新字段
- 项目文件说明.txt：补充 routes/system.js、admin/System.vue
- Linux 部署手册.html：重写为正式版（原 docx 关键命令已纳入）

## 编辑草稿界面对齐新建汇报
- **后端** `routes/report.js`：`POST /reports/edit/:id` 新增接收 `report_type`、`cycle_start`、`cycle_end`、`record_ids`，更新 cycle_report 对应字段和 report_record 关联表
- **前端** `Reports.vue` 编辑弹窗：
  - 新增汇报类型（radio-group）和周期（daterange）选择，可修改
  - 改类型/周期时清空关联记录列表，需手动点"加载工作记录"
  - AI 生成区分编辑/新建模式，使用对应上下文
  - 保存草稿/提交时传递 record_ids 到后端
  - AI 加载中 textarea 设为 disabled
  - `handleEditSaveDraft`/`handleEditSubmitDraft` 修复缺少 await 的问题
