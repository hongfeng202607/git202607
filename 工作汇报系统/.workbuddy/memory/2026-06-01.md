# 2026-06-01 工作日志

## AI配置测试连接失败排查
- **问题**：AI配置测试连接报"连接失败: fetch failed"
- **根因**：端口8080被一个无关的Python进程（PID 6480）占用，LM Studio无法监听
- **处理**：用 PowerShell `Stop-Process -Id 6480 -Force` 释放端口，重新启动 LM Studio（`F:\LM Studio\LM Studio.exe`）
- **额外发现**：数据库 ai_config 中 api_url 为 `http://localhost:8080/v1/chat/completions`（非 `127.0.0.1`），模型已从 Qwen3-1.7B 改为 gemma-4-e2b-it-uncensored

## AI调用统计缺失修复
- **问题**：系统管理中"AI调用"和"最后AI调用"显示为0/空白
- **根因**：前端调用的 `/reports/ai-generate` 路由（report.js 第319-382行）在AI生成成功时没有写入 `system_log` 操作日志。而另一个有日志的 `/api/ai-proxy/generate` 路由前端并未调用
- **修复**：在 `/reports/ai-generate` 的 AI 生成成功分支中（content 非空时），添加与 `/api/ai-proxy/generate` 一致的日志插入逻辑：`INSERT INTO system_log (..., action='ai_generate', ...)`
- **重启**：后端已重启（PID 30564→14452），改动已生效
